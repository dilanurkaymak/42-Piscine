#include <unistd.h>

char    *ft_strncpy(char *dest, char *src, unsigned int n);

int main(void)
{
    char src[] = "Merhaba";
    char dest[10];

    ft_strncpy(dest,char,5);
    printf("%d", dest);
    printf("%s", ft_strncpy(str[i]);
}
